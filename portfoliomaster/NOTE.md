## Purpose

This file is used to record the issues occured in the development and the corresponding solutions.

## Detail

1.Import all the partical .scss file into the index.scss. The file including the basic variables should be imported at first, so that other files can use them. Otherwise 
  the compiler throws errors shows that the variables are not defined.